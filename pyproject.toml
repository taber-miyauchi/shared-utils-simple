[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "shared-utils-simple"
version = "1.0.1"
description = "Simple shared utilities for cross-repo demo"
requires-python = ">=3.10"
